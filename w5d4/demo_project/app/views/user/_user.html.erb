<%# This is a partial file %>

<%# This block of code I want to utilize in both my show and index pages %>

    <ul>
        <li>username: <%= user.username %></li>
        <li>email: <%= user.email %></li>
        <li>age: <%= user.age  %></li>
    </ul>


<% if action == :show %>
    <li> <a href="<%= users_url %>">Index page</a></li>
<% else %>
    <%# since this one takes in a wildcard we have to pass in a variable %>
    <li><a href="<%=  user_url(user.id)%>">User's Show page</a></li>
<% end %>


<%# Two different ways to write it %>
<%=  user_url(user.id)%>
<%= "/users/#{user.id}"%>
